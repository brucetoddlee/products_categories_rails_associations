<h4><%= @product.name %></h4>

<h5>Categories this product is in:</h5>
<ul>
<% @cats.each do |c| %>
  <li>
    <%= link_to c.name, cat_path(c) %> 
  </li>
<% end %>

<%= button_to "Edit product", edit_product_path(@product), method: 'get' %>